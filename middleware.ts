// middleware.ts
// Temporarily disabled auth middleware for Vercel demo deployment.
// All auth is handled in layouts & server components instead.

// If you want to re-enable later, restore the `export { auth as middleware } from "./auth"` form.

// export { auth as middleware } from "./auth";

// export const config = {
//   matcher: ["/((?!api|_next/static|_next/image|favicon.ico|.*\\.png$).*)"],
// };
